#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>

/**int unsigned_int(va_list args)
{
        unsigned int num;

        num = va_arg(args, unsigned int);

        if (num == 0)
{
                return ( (num));
}
        if (num < 1)
{
                return (-1);
}
}**/
